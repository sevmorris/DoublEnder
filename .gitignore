# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
project.xcworkspace/

# macOS
.DS_Store

# DMG artifacts
*.dmg

# Claude
.claude/

# GCS service account key — client build only, never commit
DoublEnderCloud/doublender-*.json
doublender-*.json

# Per-client branded build assets — confidential. The scaffolding
# (_template/, README.md, this .gitignore) stays committed; actual
# client subdirectories are gitignored. See Brands/README.md.
/Brands/*/
!/Brands/_template/

# DoublEnder Cloud (private version)
DoublEnder Cloud/
DoublEnderCloud/
release-cloud.sh
release-cloud-branded.sh

